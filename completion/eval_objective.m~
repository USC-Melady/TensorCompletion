function [ obj_val ] = eval_objective( X,Y,Z,M,Omega, lambda, rho )
%EVAL_OBJECTIVE Summary of this function goes here
%   Detailed explanation goes here
obj_val = 0;
m = length(X);

t1 = lambda * norm(Z(Omega) -M(Omega), 'fro')^2;
for i = 1:m
    obj_val = obj_val +  trace(sqrt(A'*A));
    
    obj_val = obj_val +  rho * 
end

obj_val = t1+ t2 + t

end

